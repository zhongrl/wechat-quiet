<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quiet.live.hall.mapper.JCouponDetailMapper">

	<!-- 通用查询结果列-->
	<sql id="Base_Column_List">
		 id, title AS title, create_time AS createTime, price AS price, 
		 remake AS remake, start_time AS startTime, end_time AS endTime, 
		 jc_username AS jcUsername, js_user_id AS jsUserId, sy_username AS syUsername,
		  sy_openid AS syOpenid, del_flag AS delFlag, give_openId AS giveOpenid,
		   give_username AS giveUsername, coupon_id AS couponId,unionid AS unionid 
	</sql>

</mapper>